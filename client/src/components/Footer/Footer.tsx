import React from "react";

import styles from "./Footer.module.scss";

type Props = {};

const Footer = (props: Props) => (
    <div className={styles.footer}>{"Developed by DevinR"}</div>
);

export default Footer;
